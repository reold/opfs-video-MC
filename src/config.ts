import { PossibleVector2 } from "@motion-canvas/core";

const config = {
  colors: {
    primary: "#FEE715",
  },
  shadow: {
    primary: {
      shadowOffset: [10, 10] as PossibleVector2,
      shadowColor: "#000",
      shadowBlur: 10,
    },
  },
};

export default config;
